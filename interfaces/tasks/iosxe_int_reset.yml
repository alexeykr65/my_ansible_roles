---
# tasks file for interfaces
- name: "IOSXE INTERFACE=>> Reset Interfaces"
  ios_config:
    lines: "default interface {{ item.name }}"
  when: item.name is not search("loopback") 
  loop: "{{ interfaces }}"
  # and interfaces is defined
  
